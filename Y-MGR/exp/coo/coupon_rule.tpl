<table class="body">
 <tr>
  <td class="fa" width="90">券码类型<font color="red">(*)</font>:</td>
  <td class="fd" width="165">${TYPE}</td>
  <td class="fas" width="90">券码名称<font color="red">(*)</font>:</td>
  <td class="fd" width="300">${NAME}</td>
 </tr><tr>
  <td class="fa">票券金额<font color="red">(元)</font>:</td>
  <td class="fd">${TMA}</td>
  <td class="fas">票券比率<font color="red">(%)</font>:</td>
  <td class="fd">${TMB}</td>
 </tr><tr>
  <td class="fa">使用条件说明:</td>
  <td class="fd" colspan="3">满足以下条件方可使用</td>
 </tr><tr>
  <td class="fa">满足金额<font color="red">(元)</font>:</td>
  <td class="fd">${TOALL}</td>
  <td class="fas">满足期限<font color="red">(天)</font>:</td>
  <td class="fd">${TODAY}</td>
 </tr><tr>
  <td class="fa">有效类型<font color="red">(*)</font>:</td>
  <td class="fd">${STID}</td>
  <td class="fas">生效时间<font color="red">(*)</font>:</td>
  <td class="fd">${SDAY}</td>
 </tr><tr>
  <td class="fa">有效时长<font color="red">(天)</font>:</td>
  <td class="fd">${TDAY}</td>
  <td class="fas">截止时间<font color="red">(*)</font>:</td>
  <td class="fd">${EDAY}</td>
 </tr><tr>
  <td class="fa">发放原因<font color="red">(*)</font>:</td>
  <td class="fd" colspan="3">${REMARK}</td>
 </tr>
</table>
